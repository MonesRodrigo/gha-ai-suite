version: 2
updates:
  - package-ecosystem: github-actions
    # Composite actions live outside .github/workflows and need their own entry.
    directories: ["/", "/actions/*"]
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
